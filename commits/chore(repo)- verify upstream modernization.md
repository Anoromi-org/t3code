# Verify upstream modernization

Audit all fork commits and captured pending work against the pinned upstream main. Preserve behavioral tests, record replacements where upstream moved an implementation, and verify real-state migration and projection bootstrap on isolated copies. Update pnpm 11 Nix fetching, durable shared settings targets, shortcut documentation, and the preservation report.

Resolve review findings by recovering proposed plans from Codex history, batch-checking settled turns before ingestion, and requiring completed, registered worktrees before retry reuse. Preserve implemented-plan metadata and cover incomplete checkout recovery.
