# Server generate unique idempotent worktree branches

Generate the final worktree branch before creation, suffix occupied names, serialize competing requests, and reuse successful creation on retries. Explicit existing branches and already selected worktrees remain reusable. Web and mobile request generation only when the server advertises support, with temporary branches for older servers.

Preserve upstream attachment uploads, citation-derived titles, authenticated dispatch and deletion drains, local-only base-branch fallback, submodule checkout, and long-running removal behavior. Keep source commit a5dc00a023's generation, concurrency, retry, and client selection tests alongside the upstream regressions.
